# VoiceMemo - 実装ロードマップ

## 📊 プロジェクト概要

**売り：** 「Verbit級の高機能なリアルタイム文字起こし」 + 「チャット風UI」 + 「YouTube コメント風の動的レイアウト」

**ターゲット：** 士業、営業、人事（秘密情報重視層）

**販売方法：** Gumroad HTML版スタンドアロン（$4.99ベータ版 → $9.99正式版）

---

## 🎯 Phase 1: UI/UX 大改造（最優先）

### タスク 1.1: リアルタイム文字起こしの UI 統一

**現状：** チャットバブル式（スマホ的）
```
🙂 「月末の報告書…」
👤 「ありがとうございます」
```

**目標：** YouTube コメント + タイムスタンプ併用
```
[0:03] 🙂 自分：「月末の報告書…」 [編集] [翻訳▼]
[0:08] 👤 相手：「���りがとうございます」
```

**実装内容：**
- [ ] `addBubbleYouTube()` 関数を新規作成
- [ ] タイムスタンプ表示の追加
- [ ] 「話者アイコン + 名前」表示
- [ ] クリック時の「再生位置同期」機能
- [ ] マウスホバー時の「編集/翻訳」ボタン表示

**担当ファイル：** `voicememo_v5.html` (JavaScript section)

---

### タスク 1.2: YouTube コメント風の動的レイアウト実装

**目標：** 3つのモード
```
【モード1：動画メイン】（クリック or ドラッグで） 
┌─────────────────────┐
│                     │
│    動画 66%         │  
│                     │
├─────────────────────┤
│ テキスト 33%        │
└─────────────────────┘

【モード2：テキストメイン】
┌─────────────────────┐
│ 動画 33%            │
├─────────────────────┤
│                     │
│   テキスト 66%      │
│                     │
└─────────────────────┘

【モード3：1対1】
┌─────────────────────┐
│  動画 50%│テキスト   │
│          │50%       │
├─────────┼──────────┤
│プレイヤー│スクロール │
└─────────┴──────────┘
```

**実装内容：**
- [ ] CSS で 3つのレイアウトモード定義
- [ ] JavaScript で `setLayoutMode('video' | 'text' | 'split')` 関数実装
- [ ] 「動画領域」「テキスト領域」「分割線」をクリック可能に
- [ ] レイアウト状態を `localStorage` に保存（再訪時に復元）

**担当ファイル：** `voicememo_v5.html` (CSS + JS)

**実装例：**
```javascript
// CSS グリッドで動的レイアウト
let layoutMode = 'split'; // デフォルト

function setLayoutMode(mode) {
  layoutMode = mode;
  const detBody = document.getElementById('p-detail').querySelector('.det-body');
  
  if (mode === 'video') {
    detBody.style.display = 'grid';
    detBody.style.gridTemplateRows = '2fr 1fr';
  } else if (mode === 'text') {
    detBody.style.gridTemplateRows = '1fr 2fr';
  } else {
    detBody.style.gridTemplateColumns = '1fr 1fr';
  }
  
  localStorage.setItem('layoutMode', mode);
}

// 動画領域クリック → 動���モードに切り替え
document.getElementById('detail-video').onclick = () => setLayoutMode('video');
// テキスト領域クリック → テキストモードに切り替え
document.getElementById('det-ts').onclick = () => setLayoutMode('text');
// 分割線ドラッグ → 比率調整
```

---

### タスク 1.3: 「話者アイコン」の SNS 連携準備

**目標：** ユーザーが「自分 / 相手」の代わりに SNS アイコンを表示できる

**実装内容：**
- [ ] 設定ページに「話者情報設定」セクション追加
  - 自分の名前 + アイコン画像 URL
  - 相手の名前 + アイコン画像 URL
- [ ] 画像を `localStorage` に保存
- [ ] 文字起こし画面に表示
- [ ] ベータ版では「手動入力」のみ（将来：Google/Twitter API連携）

**UI例：**
```
┌─────────────────────────────────┐
│ 🎙️ 話者設定                    │
├─────────────────────────────────┤
│ 自分：                          │
│  [田中 太郎 ______]  [画像URL]  │
│  [アイコン選択▼]              │
│                                 │
│ 相手：                          │
│  [山田 花子 ______]  [画像URL]  │
│  [アイコン選択▼]              │
└─────────────────────────────────┘
```

**担当ファイル：** `voicememo_v5.html` (setup-sec にセクション追加)

---

## 🎬 Phase 2: リアルタイム再生との UI 統一（1週間）

### タスク 2.1: 録音中の UI を再生時と統一

**現状の問題：**
- 録音中：チャットバブル式
- 再生時：YouTube 風

**目標：** 両方を YouTube 風に統一

**実装内容：**
- [ ] 録音中の `addBubble()` 関数を YouTube スタイルに変更
- [ ] タイムスタンプをリアルタイムで付与
- [ ] スクロール挙動を再生時と同じに

---

### タスク 2.2: 動画プレビューと文字起こしの「同じ画面統一」

**現状：**
- 録音中：動画プレビュー（別枠） + テキスト
- 再生時：動画 + テキスト

**目標：** 同じレイアウトモード（動画メイン/テキストメイン/1対1）で統一

---

## 🧪 Phase 3: ユーザーテスト（1週間）

### テスト項目
- [ ] Zoom 30分会議を実際に録画
- [ ] リアルタイムプレビューは見やすいか？
- [ ] 再生時の「動画 + テキスト同期」はスムーズか？
- [ ] レイアウト切り替え（動画/テキスト/1対1）は直感的か？
- [ ] 「話者アイコン」は識別しやすいか？
- [ ] テキストのクリック → 再生位置ジャンプは便利か？

### フィードバック記録
- [ ] 改善点をメモ
- [ ] 優先度を付ける（必須/推奨/次版）

---

## 💰 Phase 4: Gumroad ベータ版リリース（即座）

### 準備物
- [ ] Gumroad アカウント作成
- [ ] 商品ページ作成
  - 説明文：「Verbit級の文字起こし + チャット風UI」
  - スクリーンショット 3枚
  - 動画デモ（30秒）
- [ ] 価格：$4.99（ベータ割引）

### リリース内容
- [ ] HTML ファイル直接ダウンロード販売
- [ ] README に使い方を記載
- [ ] 「ベータ版のため、フィードバックをお願いします」の旨を記載

---

## 🎯 実装優先度

### 緊急（Week 1）
1. **Task 1.1：YouTube 風文字起こし UI** 
   - これなしではテストが意味をなさない
   
2. **Task 1.2：動的レイアウト（3モード）**
   - あなたの最大の売り

### 重要（Week 2）
3. **Task 1.3：話者アイコン設定**
   - ベータ版では簡易版で OK

4. **Task 2.1-2.2：UI 統一**
   - ユーザーテストで必須

### その他（将来）
5. Task 3：ユーザーテスト & フィードバック収集
6. Task 4：Gumroad リリース

---

## 📝 実装チェックリスト

### Phase 1: UI改造

- [ ] Task 1.1 完了：YouTube 風文字起こし UI
  - [ ] タイムスタンプ表示
  - [ ] 話者アイコン + 名前
  - [ ] クリック → 再生位置同期
  - [ ] マウスホバー時のアクション表示

- [ ] Task 1.2 完了：動的レイアウト
  - [ ] CSS グリッド実装
  - [ ] 3モード切り替え関数
  - [ ] クリック検出 & モード切り替え
  - [ ] localStorage 保存/復元

- [ ] Task 1.3 完了：話者設定
  - [ ] 設定ページに新セクション
  - [ ] 画像 URL 入力欄
  - [ ] localStorage 保存

### Phase 2: UI統一

- [ ] Task 2.1 完了：録音中の UI 統一
  - [ ] YouTube 風に変更
  - [ ] タイムスタンプリアルタイム付与

- [ ] Task 2.2 完了：動画プレビュー統一

### Phase 3: テスト

- [ ] Zoom 実会議 30分録画
- [ ] フィードバック 5項目以上収集
- [ ] 改善点リスト作成

### Phase 4: リリース

- [ ] Gumroad ページ作成
- [ ] HTML ファイルアップロード
- [ ] 販売開始

---

## 📅 タイムライン

| 週 | タスク | 期限 |
|---|-------|------|
| W1 | Phase 1.1-1.2 | 5日 |
| W1-W2 | Phase 1.3 + Phase 2 | 7日 |
| W2 | Phase 3 ユーザーテスト | 3-5日 |
| W2-W3 | フィードバック反映 | 3-5日 |
| W3 | Phase 4 Gumroad リリース | 1-2日 |

**総期間：約3週間で「ベータ版販売開始」**

---

## 💡 実装のコツ

### CSS レイアウト（モードの使い分け）

```css
/* デフォルト：1対1分割 */
.det-body {
  display: grid;
  grid-template-columns: 1fr 1fr;
  height: 100%;
}

/* 動画メイン：2:1 */
.det-body.layout-video {
  grid-template-rows: 2fr 1fr;
  grid-template-columns: 1fr;
}

/* テキストメイン：1:2 */
.det-body.layout-text {
  grid-template-rows: 1fr 2fr;
  grid-template-columns: 1fr;
}

/* 1対1 */
.det-body.layout-split {
  grid-template-columns: 1fr 1fr;
}
```

### クリック検出と mode 切り替え

```javascript
document.getElementById('det-vid-wrap').addEventListener('click', 
  () => setLayoutMode('video'));

document.getElementById('det-ts').addEventListener('click', 
  () => setLayoutMode('text'));

// 中央の分割線をドラッグで比率調整（オプション）
```

---

## 🎁 注：あなたの次のアクション

1. **今日：** このロードマップを確認して、同意を得る
2. **明日：** Task 1.1 実装開始（YouTube風文字起こし UI）
3. **3日後：** Task 1.2 実装（動的レイアウト 3モード）
4. **1週間後：** ユーザーテスト開始

---

**応援しています。頑張ってください！** 🚀
